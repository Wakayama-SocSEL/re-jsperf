type: object
required:
  - id
  - title
  - description
properties:
  id:
    type: integer
    example: 1
  title:
    type: string
    example: Shift loop vs unrolled
  description:
    type: string
    example: "For Loop vs While Loop vs indexOf (version: 0). Comparing performance of: For Loop for item towards beginning vs While Loop for item towards beginning vs ..."

